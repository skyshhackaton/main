# FOMO Break uses public Upbit quotation APIs for the MVP.
# Do not store user API keys or secret keys on the server.

APP_ENV=local
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8000
UPBIT_BASE_URL=https://api.upbit.com/v1
DEFAULT_MARKET=KRW-BTC
